# ===========================================
# VARIÁVEIS DE AMBIENTE PARA O RENDER
# ===========================================
# Copie este arquivo para .env e preencha com seus valores reais
# No Render, adicione estas variáveis na aba "Environment"

# ===========================================
# SPRING CONFIGURATION
# ===========================================
# Perfil ativo do Spring (prod para produção)
SPRING_PROFILE=prod

# ===========================================
# DATABASE CONFIGURATION (PostgreSQL)
# ===========================================
# URL de conexão do banco PostgreSQL
# Formato: jdbc:postgresql://HOST:PORTA/NOME_DO_BANCO
# Exemplo Render: jdbc:postgresql://dpg-xxxxx.oregon-postgres.render.com/nome_do_banco
APP_DATASOURCE_URL=jdbc:postgresql://seu-host-render.postgres.render.com:5432/seu_banco

# Usuário do banco de dados
APP_DATASOURCE_USERNAME=seu_usuario

# Senha do banco de dados
APP_DATASOURCE_PASSWORD=sua_senha_segura

# Driver JDBC (não precisa mudar para PostgreSQL)
APP_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver

# ===========================================
# JPA/HIBERNATE CONFIGURATION
# ===========================================
# Dialeto do Hibernate para PostgreSQL
APP_JPA_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Estratégia de criação/atualização do schema
# Valores possíveis: none, validate, update, create, create-drop
# IMPORTANTE: Use "update" em produção com cuidado!
APP_JPA_HIBERNATE_DDL_AUTO=update

# Mostrar SQL no console (false em produção)
APP_JPA_SHOW_SQL=false

# Formatar SQL no console (false em produção)
APP_JPA_FORMAT_SQL=false

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
# URL do frontend (para CORS)
APP_URL_FRONTEND=https://seu-frontend.com

# ===========================================
# PORTA (Render define automaticamente)
# ===========================================
# Não é necessário definir, o Render injeta automaticamente
# PORT=8080

# ===========================================
# INSTRUÇÕES PARA O RENDER:
# ===========================================
# 1. Crie um banco PostgreSQL no Render
# 2. Copie a "Internal Database URL" do banco
# 3. No seu Web Service, vá em "Environment"
# 4. Adicione cada variável acima (exceto PORT)
# 5. Cole os valores do seu banco PostgreSQL
#
# IMPORTANTE:
# - Use a "Internal Database URL" se o banco estiver no Render
# - Para ddl-auto, recomendo "update" ou "validate" em produção
# - Mantenha show-sql e format-sql como "false" em produção

